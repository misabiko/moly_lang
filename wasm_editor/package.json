{
	"scripts": {
		"build": "esbuild entry.ts --bundle --minify --sourcemap --outfile=out.js",
		"dev": "npm run build && http ."
	},
	"dependencies": {
		"@codemirror/lang-javascript": "^6.1.1",
		"codemirror": "^6.0.1",
		"esbuild": "^0.15.12"
	}
}
